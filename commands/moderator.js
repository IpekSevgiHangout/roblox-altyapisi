{
  const Server = require("/server.js");
  var groupid = "GROUP ID";
  const ownersendinggroupwall = "!moderator-aç";
  const sendgroupwall = "Moderator Başarılı Şeklinde Açıldı";
  const blockscamlinks = true;
  const blockspamwords = true;
  const blockbadwords = true;
  const rankrequirement = false;
  const badwords =
    "s*a*, sca*, scam, sc*m, f*k*, fak*, fa*e, fake, r*b*x, ro*u*, rob*x, *o*u*";
  var BADWORDS =
    "s*a*, sca*, scam, sc*m, f*k*, fak*, fake, r*b*u, ro*u*, rob*x, *o*u*";
}
